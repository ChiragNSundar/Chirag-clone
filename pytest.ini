[pytest]
asyncio_mode = auto
testpaths = backend/tests
pythonpath = backend
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    voice: mark test as voice related
    vision: mark test as vision related
    knowledge: mark test as knowledge related
    asyncio: mark test as async
